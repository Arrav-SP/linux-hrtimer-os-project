# Code snippet provenance

The report includes short, exact excerpts from `src/timer_benchmark.c` and `src/cpu_load.c`.

The project does not currently contain the Linux kernel source tree described by `MASTER_CONTEXT.md`, and `kernel_module/` is empty. No kernel excerpt is copied here because doing so without a version-matched local source file would risk presenting unverifiable text as project evidence. The report records the relevant hrtimer symbols and the missing-source limitation explicitly.
